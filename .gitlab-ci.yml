include:
  - project: 'FlyingArmageddon/k8s-deploy-utils'
    ref: main
    file: '/templates/ci-auto.yml'


variables:
  K8S_NAMESPACE: zseilplan
  HARBOR_REPO: backend

  HELM_ARGUMENTS: --set cronjobs.jobs.timetablesync.image.repository=${HARBOR_BASE}/${HARBOR_REPO} --set cronjobs.jobs.timetablesync.image.tag=${HARBOR_TAG}
  HELM_RELEASE_NAME: zseilplan-backend-test
